name: optimeal
description: OptiMeal Chef Harris AI Assistant
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.0.0
  http: ^1.0.0
  crypto: ^3.0.3
  supabase_flutter: '>=1.10.0'
  google_fonts: ^6.1.0
  provider: ^6.1.2
  go_router: ^16.2.0
  purchases_flutter: ^10.8.0
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  path_provider: ^2.1.5
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true